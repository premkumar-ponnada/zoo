# Generation scratch: raw clips, stills, prompt files, the Python generators.
# 62 MB and nothing here is fetched by the site.
build/

# Playwright artefacts from QA
.playwright-mcp/

# QA screenshots taken during development. Root-scoped on purpose: a bare *.png
# would also drop assets/logo/favicon.png, which index.html references.
/*.png

# The logo master. Only assets/logo/logo.webp (13 KB) is referenced; this is the
# 460 KB source it was cut from.
assets/logo/logo.jpg

# Generated by the earlier placeholder passes
*.log
